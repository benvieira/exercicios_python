import math
n = int(input('Digite um número: '))
raiz = math.sqrt(n)
print('A raiz de {} é igual a {}'.format(n, math.floor(raiz)))

# importando a biblioteca 'math' trás funcionalidades extras:
# ceil (arredondamento para cima)
# floor (arredondamento para baixo)
# trunc (eliminar da virgula pra frente)
# pow (potência)
# sqrt (raiz quadrada)
# factorial (calcular fatorial)

#Criar um programa que leia um número real qualquer e mostre na tela somente sua posição inteira.
